<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

  <!-- Pick up AccessTokenController, AuthorizeController, parsers, etc. -->
  <context:component-scan base-package="org.oscarehr.ws.oauth"/>

  <!-- Exception mapper so we return 401/PlainText for OAuth errors -->
  <bean class="org.oscarehr.ws.oauth.OAuthExceptionHandler"/>

  <!-- View resolver for /login/3rdpartyLogin and /login/authorized -->
  <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
    <property name="prefix" value="/WEB-INF/jsp/"/>
    <property name="suffix" value=".jsp"/>
    <property name="order"  value="1"/>
  </bean>
</beans>
