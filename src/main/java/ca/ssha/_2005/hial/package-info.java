/*
 * JAXB types for the OLIS response payload (the inner XML content of
 * HIALResponse.SignedResponse.SignedData, distinct from the SOAP envelope
 * which is defined by archive/olis.wsdl and stubbed by OLISStub).
 *
 * Originally generated once by xjc in 2011 against an external schema that
 * was not retained in this repo. Ported from the pre-compiled
 * ca.ssha.www:olis-service:20111111 jar when that jar was retired as part of
 * the A5 axis2-stub regeneration.
 * These classes don't depend on axis2, so they survive axis2 version bumps
 * cleanly — they were retired alongside the stub purely because they
 * happened to live in the same jar.
 */
@javax.xml.bind.annotation.XmlSchema(namespace = "http://www.ssha.ca/2005/HIAL")
package ca.ssha._2005.hial;
